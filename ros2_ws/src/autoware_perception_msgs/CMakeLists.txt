cmake_minimum_required(VERSION 3.8)
project(autoware_perception_msgs)

# CURATED INTERFACE PACKAGE — real Autoware name, verbatim PredictedObjects
# closure. The msg/ file list below is REGENERATED by
# scripts/curated_interface/extract_closures.sh from the reference Autoware
# install — do not hand-edit it, and do not hand-edit the .msg files.

find_package(ament_cmake REQUIRED)
find_package(rosidl_default_generators REQUIRED)
find_package(std_msgs REQUIRED)
find_package(geometry_msgs REQUIRED)
find_package(builtin_interfaces REQUIRED)
find_package(unique_identifier_msgs REQUIRED)

rosidl_generate_interfaces(${PROJECT_NAME}
  # >>> CURATED_MSG_LIST (regenerated by extract_closures.sh — do not hand-edit) >>>
  "msg/ObjectClassification.msg"
  "msg/PredictedObjectKinematics.msg"
  "msg/PredictedObject.msg"
  "msg/PredictedObjects.msg"
  "msg/PredictedPath.msg"
  "msg/Shape.msg"
  # <<< CURATED_MSG_LIST <<<
  DEPENDENCIES std_msgs geometry_msgs builtin_interfaces unique_identifier_msgs
)

ament_package()
