# ADAS-Benchmark Gap-Closure Execution Plan — Claude Code Track

**Date:** 2026-07-07 (re-baselined 2026-07-08 — see §0)
**Basis:** `docs/analysis/ADAS_BENCHMARK_GAP_ANALYSIS.md` (MGA G-1…G-22, 2026-07-06; the historical `MGA` gap-ID prefix is retained — code and docs cross-reference `MGA G-x` throughout).
**Scope rule:** this plan covers ONLY work executable with Claude Code in this repository — code, tests, CI, and evidence artifacts that build and verify in a Linux sandbox. **Certification-process items (ASIL-D assessment, TÜV engagement, IEEE/standards body work, Ferrocene licensing) and hardware/license-blocked items (QNX Hypervisor, target-silicon WCET, Jetson CI runner, sensor procurement) are separate tracks** and appear here only where a work package produces their *software half* so the external track starts unblocked.
**Tracking:** on completion, each work package (WP) updates `docs/analysis/archive/GAP_CLOSURE_STATUS.md` conventions — a row naming its evidence (test/fn/CI job), and the MGA gap it closes or partially closes.

---

## 0. Verified-status re-baseline (2026-07-08) — READ THIS FIRST

> **Forward plan:** the executable successor program derived from the DD review — dependency graph, EP-xx work packages, milestones M1–M5, and the retirement list — is `docs/analysis/ENGINEERING_EXECUTION_PROGRAM.md`. New work is scheduled THERE; this document remains the WP-01…24 historical record + verified statuses.

An independent, evidence-based due-diligence review (`docs/analysis/ADAS_BENCHMARK_DD_REVIEW.md`) verified every WP claim below against the actual code, tests, and CI — not this document. Its corrections are AUTHORITATIVE over the per-WP status rows:

- **"DONE" below means "CORE DONE" for six items** whose implementations exist and are tested but have **no live consumer yet**: WP-19's lease (the live promotion loop still runs the legacy 10 s heartbeat — the ≤5 s failover property is NOT yet delivered), WP-22's EVT/pWCET (zero callers), WP-24's `model_lineage`/`ood`/`model_targets`, WP-18's `EpochFence`/`NodeStore` traits (not consumed generically) and the Postgres migration backend (mock executor only — no real PG driver exists).
- **Never started:** WP-11 (curved-geometry RSS), WP-21 (the in-line SHM enforcement path — the software half of Critical gap G-1), WP-21b (zero-copy production adoption).
- **Verified gap closure:** ✅ 3 fully (G-10, G-13, G-20) · 🟡 7 mostly · 🟠 9 partially · 🔴 3 open (G-2, G-5, G-22 — external by design). Weighted by severity: **≈ 45 % of the full gap set, ≈ 70 % of the in-repo software scope.**

> **Progress update (2026-07-08, M1+M2-partial):** the six orphan cores are now WIRED (EP-04/05/06 + EP-21: lineage → live ORT load, OOD → live per-tick feed, model_targets → `kirra-ota-ctl model-allowlist`, EVT → wcet-bench). **WP-19 final is DELIVERED (EP-03):** the lease flip is live behind `KIRRA_HA_LEASE_ENABLED` and the two-process failover drill (`tests/ha_two_process_drill.rs`, EP-02) measures **≤5 s failover with the gate on** (≈4.6 s observed) plus the legacy path within its bound; the drill also caught and fixed a post-failover self-fence (every promoted standby demoted itself on its first heartbeat tick). Gate remains default-OFF pending a release decision. Items 2, 3, and 7 below are done; item 1 (WP-21/EP-01) is next.

**Re-prioritized roadmap (supersedes the "Wave 3 remaining" line in §6a; ROI-ranked):**
1. **WP-21 — in-line SHM enforcement path** (every prerequisite core exists; integration, not invention; unblocks G-1's software half and gives G-18 a consumer).
2. **WP-19 final — live lease flip** behind an env gate, validated by a scripted two-process failover drill (epoch fence stays the backstop; never default-on without the drill).
3. **WP-24 wiring — the three orphan ML cores**: lineage → ORT `load_model` rollback; OOD fed per-tick confidences in `run_pipeline_tick`; node sets `KIRRA_MODEL_ALLOWLIST` (+`_STRICT`) from the verified signed manifest.
4. **True MC/DC** (resolve issue #65; the coverage job currently falls back to branch coverage).
5. **WP-11 + RSS constant validation** (the algebra is done; the certified *numbers* are not — needs a safety engineer in the loop).
6. **One real Postgres driver adapter + PG service container in CI** — then STOP extracting store traits (wire-or-delete rule: no new domain trait without a consumer or the live-PG milestone).
7. Small: consume EVT in `kirra-wcet-bench`; weekly deep-fuzz schedule; rename the two power-loss drills distinctly.

**Retired from the roadmap (with cause):** `SchedulingClass`→syscalls under tokio (wrong layer — re-homed to the QNX/EPIC-#270 dedicated-thread lane; the enum stays as declarative metadata) · whole-store `VerifierStorage` trait over all ~130 methods (extract on demand only) · G-22 AUTOSAR (customer-driven; revisit on a signed requirement) · CRL-file at the TLS callback (revocation already works).

---

## 1. Tractability triage of the 22 MGA gaps

| MGA gap | Claude-Code tractable? | What closes here / what stays external |
|---|---|---|
| G-1 in-line RT enforcement | **Partial** | Build + host-verify the SHM in-line governor path (WP-21); QNX/target validation external |
| G-2 hypervisor FFI | **No** | License + hardware campaign. WP-21/WP-01 keep the software side ready |
| G-3 certified WCET | **Partial** | EVT/MBPTA analysis + reporting in kirra-timing (WP-22); target data external |
| G-4 perception/world model | **Partial** | Feed pedestrian-RSS from taj (WP-10); real sensors/D1 hardware external |
| G-5 independent assessment | **Excluded** (user carve-out) | Doc-consistency WP-06 reduces assessor friction, that's all |
| G-6 tool qual + MC/DC + mutation | **Mostly** | Mutation gate, run-to-fuzz, coverage gating, fmt lane (WP-04, WP-08); Ferrocene external |
| G-7 Uptane OTA + keys | **Yes** | Signature-verified staging, role metadata, rotation (WP-12/13/14) |
| G-8 secure/measured boot | **Partial** | Provisioning tooling + quote-required policy path (WP-16); device enrollment external |
| G-9 fleet-scale store | **Yes** | Store trait + second backend + lease (WP-18/19) |
| G-10 rate limit / tracing / histograms | **Yes** | WP-02/03/05 |
| G-11 execution manager | **Yes** (Linux-testable) | WP-20 |
| G-12 RSS lateral/geometry | **Yes** (constants stay VALIDATION-PENDING) | WP-07, WP-11 |
| G-13 seqlock ordering + loom | **Yes** | WP-01 |
| G-14 divergence→posture | **Yes** | WP-09 |
| G-15 ML lineage/OOD | **Yes** (software: model-integrity allow-list + lineage rollback-to-last-good + OOD/PSI posture-escalation core; signed-manifest + live wiring + Jetson CI runner external) | WP-24 |
| G-16 statistical validation | **Yes** (software half: 10⁴–10⁵ seeded MC corpus + Wilson/Clopper-Pearson CI gate, per-PR + nightly; CARLA runner external) | WP-23 |
| G-17 config unification | **Yes** | WP-17 |
| G-18 zero-copy adoption | **Partial** | Feature-gated iceoryx2 integration, host-tested (WP-21b) |
| G-19 cert lifecycle | **Yes** | WP-15 |
| G-20 migration framework | **Yes** | WP-18 (bundled) |
| G-21 faster failover | **Yes** | WP-19 |
| G-22 AUTOSAR interop | **Deferred** | Customer-driven; not scheduled |

Net: **17 of 22 gaps have a fully or substantially closable software component in this repo.**

---

## 2. Execution protocol (applies to every WP)

1. **One WP = one branch = one PR**, named `claude/wp-NN-<slug>`. Small enough to review in one sitting; split if >~800 diff lines.
2. **Invariants:** CLAUDE.md's 13 security invariants and the doer-checker invariants are non-negotiable gates on every WP. Fail-closed is the default answer to every design fork.
3. **Evidence-first DoD.** A WP is done when: (a) `cargo test --workspace` + affected `-p` suites green; (b) new behavior has a *negative* test (the fault/abuse case, not just the happy path); (c) safety-relevant code carries `// Verifies: SG-NNN` / `// SAFETY:` tags so the auto-generated RTM picks it up; (d) `ci/check_quality_guardrails.py` passes (panic budget, line ceilings); (e) the WP's row lands in `GAP_CLOSURE_STATUS.md`.
4. **No silent claim inflation.** Anything a WP cannot verify in-sandbox (target timing, real TPM, GPU) is labeled the way the repo already labels it (`INDICATIVE`, `VALIDATION-PENDING`, self-skipping test) — never asserted.
5. **Session sizing** below is in Claude Code working sessions (S ≈ 1, M ≈ 2–4, L ≈ 5–10 sessions), assuming review turnaround between PRs.

---

## 2a. Execution status

| WP | Status | Evidence |
|---|---|---|
| WP-01 | **DONE** 2026-07-07 (`a04c454`) | Paired fences in `seqlock.rs` (driver-owned, all bindings inherit); loom model `seqlock_accepted_snapshot_is_never_torn` (0.6 s, preemption bound 2); EACH fence's necessity verified by removal (loom reports the torn accept); 68 dependent-crate tests green |
| WP-02 | **DONE** 2026-07-07 (`527afb9`) | Limiter wired into `recv_report`/`recv_and_ingest` pre-verify; `flood_is_rate_limited_before_the_signature_verify` proves the excess rejects RateLimited (not BadSignature) over a live Zenoh link with `bad_signature == 0`; 32/32 crate tests |
| WP-03 | **DONE** 2026-07-07 (`ce3b808`, locks `7cdb7d0`) | Two isolated pools (API 512 / console 64) + load-shed 429+Retry-After + 256 KiB body cap, probes exempt; deterministic shed/recovery + 413 tests; posture-gate ordering re-proven on the new assembly; env vars in CLAUDE.md |
| WP-04 | **DONE** 2026-07-07 (`413aa3d`) | Advisories GATE both workspaces (RUSTSEC-2025-0134 triaged with reason); fuzz lane runs every target 60 s (verified locally: 4/4 clean, 3.9M–47M execs); codecov.yml blocking project ratchet (auto+0.5%). fmt lane DEFERRED: ~3,900 unformatted files need a dedicated mechanical reformat commit first |
| WP-05 | **DONE** 2026-07-07 (`74dc084`) | `LatencyHistogram` (lock-free, fixed buckets) → `kirra_http_request_duration_seconds` + `kirra_actuator_envelope_duration_seconds` on /metrics; outermost request-observability middleware (span + x-kirra-request-id, hostile ids replaced); full workspace suite green (2,481 at that commit — the pre-Wave-0 baseline the safety docs cite is 2,476; the count grows as WPs add tests, so derive the live figure from `cargo test --workspace`, never carry it forward) |
| WP-06 | **DONE** 2026-07-07 (`7078ad7`) | Stale "306 passing" purged from 5 docs (real figure 2,476, machine-derived, do-not-hand-carry note); AEGIS-* ID lineage note added to SAFETY_CASE_INDEX §1 (deliberate cert lineage, not un-rebranded — renaming would break code cross-refs). Goal-scheme unification found already addressed by SAFETY_GOALS.md's canonical-scheme section |

| WP-07 | **DONE** 2026-07-07 | `lateral_safe_distance_split` (parko-core): IEEE 2846 §5.2 accel/brake roles separated + explicit additive μ; legacy fn = exact `split(a,a,rt,0)` wrapper (delegation-pinned test). Wired at ALL production call sites: kirra-trajectory snapshot + predictive passes (`RSS_LAT_BRAKE_FRACTION=0.7`, `RSS_MU_LATERAL_M=0.2` — both VALIDATION-PENDING; fraction-form composes conservatively with profile/derate), parko scene RSS (`RssParams.lat_brake_min`/`mu_lateral_m`, courier constants). Strict-conservatism property test (split ≥ legacy on every input) + fail-safe tests for invalid brake/negative μ; full workspace 2,486 green incl. the KPI-gate corpus. Spec §4 row updated. DEFERRED to WP-11: parameterizing the 2.5 m `RSS_LONGITUDINAL_OVERLAP_M` footprint gate per class (the per-class alignment band `rss_lateral_alignment_tolerance_m` already exists on `VehicleConfig`; the overlap gate joins the WP-11 geometry work) |

| WP-09 | **DONE** 2026-07-07 | S-DG1a/b + c-adapter implemented per STAGE_S-DG1 (all three recommended decisions adopted: reuse the comparator accumulator as significance, immediate-Degraded, DI sink). Verifier: `GovernorDivergence` reason+trigger, `apply_governor_divergence_state` (immediate Degraded, comparator-owned escalation → STICKY LockedOut, agreement-streak recovery), composed into Degraded/LockedOut clauses AND the #688 sticky guard. parko-kirra: `PostureSignalSink` seam (None = byte-identical), emission mapping tests; `PostureEngineSenderSink` adapter (try_send, fail-safe drop semantics, `verifier-sink` feature). Full §5 test matrix + end-to-end worker test (trigger → Degraded → sticky LockedOut surviving healthy recalc). REMAINING before ENFORCED (recorded in the doc): live parko-ros2 node wiring (remote-verifier topology needs a trigger transport or embedded engine). 2,499 workspace + 179 parko-kirra tests green |

| WP-08 | **DONE** 2026-07-07 | cargo-mutants gate on the checker: CI `mutation-gate` lane runs `--in-diff` over the PR's checker diff (new code must kill its mutants; non-checker PRs skip in seconds); test scope pinned in `.cargo/mutants.toml` (checker + adapter validation suite — measured: own-tests-only overstates survivors 318 vs 202). Baseline debt register `docs/testing/MUTATION_BASELINE.md` + 198-entry snapshot, shrink-only ratchet with triage policy. Starter kill: the ego-frame rotation cluster (`validation.rs:496`×3 + `415:38`) via a 45°-heading cut-in test — every prior cut-in test sat at the sin=0 degenerate point. parko-core RSS = next crate under the gate (own workspace, follow-up) |

| WP-10 | **SUBSTANTIALLY DONE** 2026-07-07 | The pedestrian-RSS PRODUCER: cluster extent threaded through Taj Phase-A (`cluster_objects_with_extent`/`process_parts` — contracts untouched), `Track` gains extent+persistence, `TajTracker::classify_pedestrians` (uncertainty promotes TO pedestrian; large/fast → vehicle-only; classified peds stay objects — strictly additive). `PerceivedPedestrian` moved to kirra-core (re-exported from `kirra_trajectory::vru`, all paths unchanged) so producer+checker share the lean contract. AdaptorState pedestrian channel with enabled-but-silent/stale/poisoned → fail-closed snapshot semantics. Evidence: 6 classifier tests incl. the end-to-end proof (Taj-classified kerbside ped → checker refuses an 8 m/s pass admitted without the channel) + 4 channel tests. REMAINING (recorded in PEDESTRIAN_RSS.md): the ros2-gated node.rs subscription + env gate + MRC-on-None consumption — CI-ros2-lane work; params VALIDATION-PENDING |

Wave 1 complete (WP-07 ✓ → WP-09 ✓ → WP-08 ✓ → WP-10 ✓ substantially — node wiring recorded).

## 5a. Wave 2 status

| WP | Status | Evidence |
|---|---|---|
| WP-12 | **DONE** 2026-07-07 | Signature-verified OTA staging (first Uptane slice). `kirra_release_token::artifact_release` — domain-separated sign/verify over the artifact digest (own tests incl. wrong-key/replay/malformed fail-closed). Installer: `with_release_key` provisions the key → `stage_verified_signed` (verify sig THEN digest); once provisioned, the unsigned `stage` is refused (`SignatureRequired`) — hash-only is the rejected legacy mode. Signature flows verifier→node: `Campaign.artifact_signature_b64` (store column + ADD-COLUMN migration + create route), `NodeAssignment`/`AssignmentView`/`PullAction` carry it, `kirra-ota-ctl pull` verifies before download (`--release-pubkey`/`KIRRA_OTA_RELEASE_PUBKEY`; provisioned-but-unsigned → refuse, unprovisioned → warn). Tamper suite (installer, 7 tests) + end-to-end drill extension (`signed_campaign_flows_the_release_signature_to_a_verifying_node`): signed→commit, forged→refuse, unsigned→refuse. 2,510+ tests green. NOTE: the installer checks a real SIGNATURE now (not just a hash) — the MGA G-7 "installer verifies a hash not a signature" finding is closed; full Uptane role separation is WP-13 |

| WP-13 | **CORE DONE** 2026-07-07 | The Uptane four-role metadata model (`kirra_release_token::uptane`), pure + fully tested: root/targets/snapshot/timestamp role-separated keys with per-role signing domains; `verify_update` timestamp→snapshot→targets pipeline enforcing role-separated signatures, freshness/expiry, rollback (version floor), and chain-consistency (mix-and-match refusal); `verify_root_rotation` (strictly-increasing, signed by BOTH outgoing+incoming root keys — compromise recovery). 13 tests, one per security property (targets-key-can't-forge-timestamp, rollback-refused, expired-refused, two chain-mismatch, three rotation, self-signed). Shared base64 factored out of the WP-12 module (no new deps). Design + follow-up recorded in docs/ota/UPTANE_ROLES.md. REMAINING (recorded): durable metadata store, campaign-engine wiring (the WP-12 `artifact_signature_b64` becomes the targets-role signature), on-device client, key custody — WP-14 continues |

| WP-14 | **DONE** 2026-07-07 | Uptane key-rotation OPERATIONS + node-side durable trust. `kirra_release_token::uptane`: `SignedRoot` bundle + `author_initial_root` / `author_root_rotation` (mints a rotation signed by both root keys — the compromise-recovery mint) / `apply_root_rotation`; metadata types gain optional serde derives (feature-gated). `kirra_ota_installer::UptaneTrustStore`: JSON trust file (adopted root + version floor), atomic temp-fsync-rename write, fail-closed load with re-verify. REVOCATION proven end-to-end AND across a restart: `adopted_rotation_revokes_the_old_key_across_a_reload` — after a persisted targets-key rotation, a reload refuses the old key's metadata and accepts the new (leaked key dead on adoption, no re-flash). 7 tests (3 authoring/revocation in release-token, 4 trust-store incl. tamper-fails-closed). REMAINING (recorded): HSM/TPM root-key custody, the audit-chained rotation route on the verifier, and the kirra-ota-ctl pull wiring |

| WP-15 | **DONE** 2026-07-07 | mTLS cert LIFECYCLE (expiry + renewal + revocation + observability). `cert_principals` gains a nullable `not_after_ms` (X.509 notAfter; ADD-COLUMN migration, back-compat NULL = never ages out) surfaced through a dedicated `CertPrincipalRecord` (`is_expired(now)`/`is_valid_at(now)`). The mTLS auth resolution (`auth.rs`) fail-closes a cert at/past its expiry exactly as on revocation → 401, with a distinct expired-cert WARN. `POST /system/cert-principals` accepts `not_after_ms` (must be future); renewal = re-pin the same principal with the renewed leaf + later expiry (in place, clears revocation, no restart). Observability: `cert_expiry_monitor` (hourly census → WARN + hash-chained `CertPrincipalExpiryWarning` audit for lapsed/lapsing certs) + the `kirra_cert_principals{state=…}` /metrics gauge family (posture-exempt) via `cert_expiry_summary`/`cert_expiry_prometheus`. Evidence: `tests/cert_lifecycle.rs` (valid-before/expired-at-and-after, renewal-restores-without-restart, revocation-honored, untracked-never-ages) + store expiry/summary tests + monitor audit tests + metrics-exposition tests; 806 lib + 109 bin + 4 lifecycle green, clippy clean, guardrails re-pinned (auth.rs 488→506, bin 4890→4902; both panic-budget-unchanged). CRL-file at the TLS callback recorded as follow-up (explicit-revocation + expiry are the live enforcement). Doc: TRANSPORT_SECURITY.md §4 cert-lifecycle subsection |

| WP-16 | **DONE** 2026-07-07 | Measured-boot software completion (software half of G-8). Two halves: (1) VERIFIER fleet gate — `KIRRA_ATTEST_REQUIRE_QUOTE_DEFAULT`: `RegisterNodeRequest.require_tpm_quote` became `Option<bool>` so an OMITTED field defers to the fleet default (pure `require_tpm_quote_fleet_default` parser + `resolve_require_tpm_quote` = `explicit.unwrap_or(default)`), an EXPLICIT value always wins, gate defaults OFF (byte-identical back-compat). (2) NODE one-call provisioning — `kirra-ota-ctl enroll`: derives the AK public SPKI PEM from the node's PKCS#8 private (which never ships; or `--ak-pub` directly), takes the expected PCR16 value (`--pcr16`, read offline via `tpm2_pcrread`/swtpm → sandbox-testable, no chip), and POSTs AK+PCR16+`require_tpm_quote=true` to `/attestation/register` in one audited call (defaults quote-required; `--no-require-quote` opts out; a non-201 is a HARD failure). Evidence: verifier — 2 pure-fn unit tests + 3 register-handler policy tests + the end-to-end `enroll_via_register_handler_then_quote_attests_end_to_end` (enroll via the real handler → challenge → generated `marshal_pcr16_quote` quote → verify → Trusted; no-quote → 403); installer — 5 enroll opts/body/PEM-derivation/pcr16 tests. 115 bin + 5 ctl tests green, clippy clean, guardrails re-pinned (bin 4902→5090, panic 142→148 — all +6 panics in `#[cfg(test)]`). Also corrected the stale "consume nonce BEFORE validation" line in `docs/v1_tpm_attestation_proof.md` §7 to the implemented VERIFY-THEN-CONSUME (nonce-preserving retry) and added §8 (enrollment + env gate). EXTERNAL remainder (hardware track): Orin Secure Boot + dm-verity + physical TPM AK enrollment (quote sig Ed25519 today → RSA/ECC on real TPM) |

Next: Wave 2 complete (WP-12 → WP-16). Wave 3 (platform & scale) started.

## 6a. Wave 3 status

| WP | Status | Evidence |
|---|---|---|
| WP-17 | **SUBSTANTIALLY DONE** 2026-07-07 (spine) | Config-unification spine (G-17), additive + low-risk. New `src/env_config.rs`: `KIRRA_ENV_KEYS` — the canonical machine-readable registry of every verifier `KIRRA_*` var (name + required + purpose, the single source of truth the CLAUDE.md table now mirrors); `unknown_kirra_env_vars` — a pure WARN-ONLY sweep (observability, never fails startup) flagging any `KIRRA_*` in the environment the registry does not know (typo / stale var); versioned `EffectiveConfig` (`CONFIG_SCHEMA_VERSION`) — a boot-config snapshot with a deterministic, change-sensitive SHA-256 `effective_digest` (no secret bytes; only whether a key source is configured). Startup wiring (`kirra_verifier_service.rs` main, post-audit-writer): WARNs on unknown vars + commits an `EffectiveConfigDigest` audit event so an operator can prove what config an instance booted under and detect drift across restarts. Evidence: 7 env_config unit tests (registry no-dupes/coverage, unknown-sweep, defaults+normalization, TLS/mTLS on-conditions, one-true flag convention, digest determinism+change-sensitivity+no-secret) all pure (no `set_var`, INV #13) + `tests/config_digest.rs` (digest committed → `audit_chain_len`+1, chain `verify_audit_chain_integrity` intact, a config change commits a different digest). 815 lib + 117 bin + 1 integration green, clippy clean, guardrails re-pinned (bin 5148→5196, panic UNCHANGED). RECORDED FOLLOW-UP (Slice B, mechanical + versioned): route the per-module env READS (verifier.rs's three `from_env` structs, standby_monitor, backpressure, the industrial adapters, the TLS resolver) THROUGH `EffectiveConfig` field-by-field so it becomes the single loader every module borrows from (each fold bumps `CONFIG_SCHEMA_VERSION` as the captured set grows) + a generated ENVIRONMENT.md from the registry. This slice lands the single-source-of-truth + observability + audit-digest without a big-bang refactor |

| WP-18 (slice 1/3) | **DONE** 2026-07-07 (migration framework) | Versioned SQLite schema migration framework (G-20), the first of WP-18's three slices (framework → trait extraction → PG backend). New `src/verifier_store/migrations.rs`: keys off `PRAGMA user_version` (never used before — every existing DB reads 0, so adoption is purely additive). `SCHEMA_VERSION` (=1, the current baseline DDL); pure `decide_migration` (UpToDate / Migrate / RefuseFuture); `assert_schema_not_future` — the FAIL-CLOSED gate `new()` runs BEFORE the DDL, refusing a DB a newer binary migrated past this one (downgrade protection, the direction VERSIONING_POLICY's safety-asymmetry clause implies); `run_migrations` — applies the ordered registered `MIGRATIONS` (empty at the v1 baseline; a future v2 change registers a `Migration` step) and stamps `user_version` to `SCHEMA_VERSION`, so a fresh/pre-framework (v0) DB is upgraded in place + stamped. `VerifierStore::schema_version()` surfaces it. Evidence: 5 unit tests (decide directions, fresh→stamp, future→refused, steps-apply-in-order via a synthetic registry, older-step-not-re-run) + `tests/schema_migration.rs` (through the real `new()`: fresh stamps baseline + data survives reopen; a forced v0 DB upgrades in place with data intact; a forced future DB is REFUSED with the downgrade-protection message). 120 store-lib + 5 migration + 3 drill + ha_failover green; clippy clean; guardrails satisfied (no line-tracked file touched). Note: `tests/audit_chain_prefix_on_kill.rs`'s SIGKILL/reexec drill fails IDENTICALLY on clean main in this sandbox container (a fork/fs quirk — "got 0"), unrelated to this change. RECORDED FOLLOW-UP (slices 2/3): the `VerifierStorage` trait extraction over `StoreHandle` (preserving INV-12 disk-before-memory + the epoch fence) and the Postgres backend behind a feature (reproducing the fence transactionally via `SELECT … FOR UPDATE`; needs a PG service container in CI). |

| WP-18 (slice 2/3) | **DONE** 2026-07-08 (migration trait + PG backend) | The migration-engine TRAIT EXTRACTION + a **Postgres** migration backend — WP-18's slices 2 and 3, scoped to the SCHEMA-VERSIONING engine (the broader whole-store `VerifierStorage` trait over every table remains the heavier deferred piece, see below). Refactored `src/verifier_store/migrations.rs`: the migration POLICY (registry ordering, fail-closed downgrade protection, apply-in-order, atomic version stamp) is now a dialect-INDEPENDENT engine — `SchemaBackend` (the seam: `read_version`/`apply_and_stamp`/`stamp` + `future_error`/`malformed_error`, with a per-backend `Step` because DDL is dialect-specific), pure `validate_step_versions`, and `run_migrations_generic<B: SchemaBackend>`. The SQLite path is now just one backend (`SqliteBackend` over `PRAGMA user_version` + `unchecked_transaction`); `run_migrations_with` delegates to the engine, so the live `VerifierStore::new` path is byte-for-byte behaviour-preserving (its public `run_migrations`/`assert_schema_not_future`/`read_user_version` signatures are unchanged). New `src/verifier_store/migrations_postgres.rs`: `PostgresBackend<E: PgExecutor>` — the SAME engine + the SAME fail-closed guarantees over a Postgres `kirra_schema_version` table (each step's DDL + its version upsert commit in ONE `transaction`). It carries **no `tokio-postgres` dependency** (which the on-device SQLite build must not drag in): it runs its SQL against an injected `PgExecutor` seam (`execute`/`query_version`/`transaction`), which an integrator binds to their driver in a ~10-line adapter. Evidence: 14 migration tests green (7 SQLite unchanged + 7 Postgres over an in-memory modelled executor: fresh→stamp, future→refuse, ordered apply, ATOMIC rollback on a mid-tx failure, malformed-registry refusal, older-step-not-re-run, upsert-format guard); 845 verifier lib total green (refactor disturbs nothing); clippy clean; guardrails untouched (`verifier_store/` is not line-tracked). RECORDED FOLLOW-UP: the whole-store `VerifierStorage` trait over `StoreHandle` (all tables, INV-12 disk-before-memory + the epoch fence) and binding `PgExecutor` to a concrete driver against a live PG service container in CI (the transactional epoch fence via `SELECT … FOR UPDATE`). |

| WP-18 (3/3) | **DONE** 2026-07-08 (epoch-fence storage trait) | The first `VerifierStorage`-family trait lifted off `VerifierStore` (G-9 store half): the backend-portable **HA epoch fence** — the "who owns writes" authority that WP-19's lease builds on, and the group with the explicit cross-backend constraint. New `EpochFence` trait in `src/verifier_store/epoch.rs` (`current_epoch`, `current_active_holder`, `try_claim_epoch` CAS, fail-closed `assert_actuator_epoch_held → FenceError`). NON-BREAKING: the trait methods share the inherent method names, and inherent methods win Rust method resolution, so every existing `store.current_epoch()` caller is untouched and the `impl EpochFence for VerifierStore` delegates via `self.method()` without recursion (documented). A second backend — `InMemoryEpochFence` — models the `ha_state` singleton (epoch + holder + present/absent) and realizes the SAME CAS + fail-closed fence semantics without a DB, as the portability-proof reference (a future Postgres backend conforms to it via `SELECT … FOR UPDATE`). Evidence: a generic `assert_fence_contract<F: EpochFence>` conformance suite run IDENTICALLY against BOTH backends (the miniature of the plan's "store suite against both backends") — proving AT-MOST-ONE-WRITER end to end (lost claim never advances the epoch; winner holds; stale/never-claimed(held==0)/future holder fenced; a second instance's claim supersedes the first; double-claim loses) + an unreadable-row (`EpochUnreadable`) test on both; 849 verifier lib total green (the inherent-preferred delegation disturbs no caller); clippy clean; guardrails untouched. RECORDED FOLLOW-UP: extend the trait family to the remaining store domains (nodes/audit/posture/… — the bulk of the ~130 methods) and bind a live Postgres backend (its epoch fence via `SELECT … FOR UPDATE`) against a PG service container in CI. |

| WP-18 (store trait 2/N) | **DONE** 2026-07-08 (node-registry storage trait) | The SECOND `VerifierStorage`-family trait after `EpochFence`: the node IDENTITY registry CRUD. New `NodeStore` trait in `src/verifier_store/nodes.rs` (`save_node` upsert, `load_node`, `load_nodes`, `node_exists`, `count_nodes`). Same non-breaking discipline as `EpochFence`: the trait methods share the inherent names, inherent methods win resolution → every existing `store.save_node(...)`/`load_node(...)` caller is untouched and `impl NodeStore for VerifierStore` delegates via `self.method()` without recursion. A second backend `InMemoryNodeStore` (a `Mutex<HashMap>` keyed by `node_id`, interior-mutable to match the trait's `&self` writes) realizes the SAME upsert/load/count semantics without a DB. Evidence: a generic `assert_node_store_contract<S: NodeStore>` conformance suite run IDENTICALLY against BOTH backends — empty reads, save→load roundtrip (id + status preserved), existence/count, bulk load, and the UPSERT invariant (re-saving an id overwrites, never duplicates); 859 verifier lib total green (delegation disturbs no caller); clippy clean; guardrails untouched (`verifier_store/` is not line-tracked). RECORDED FOLLOW-UP: continue the family to the remaining domains (audit / posture / federation / attestation / …) and bind a live Postgres backend against a PG service container in CI. |

| WP-19 (slice 1) | **DONE** 2026-07-07 (lease timing model) | Lease-based failover TIMING model (G-21), the bounded low-risk first slice (the split-brain-critical `standby_monitor` promotion loop is NOT rewritten — the proven epoch-CAS fence + heartbeat writer stay intact; live-loop wiring is recorded follow-up). New `src/lease.rs`: `LeaseParams::from_ttl` derives all timings from ONE TTL — renew at half-life (`ttl/2`, so one missed renewal survives), holder self-demotes at lease expiry (`ttl`), challenger promotes only at `ttl + ttl/2` (a full TTL to expire + a half-TTL guard). `DEFAULT_LEASE_TTL_MS = 3s` → 4.5s promote deadline (meets the ≤5s failover target, ≤ `POSTURE_CACHE_TTL_MS`, a big cut from the legacy ~12s). Pure holder/challenger decisions (`should_renew`/`lease_expired`/`should_promote`/`poll_fast_enough`) over injected monotonic elapsed-times (skew-immune, like `HeartbeatFreshness`). The `demote_before_promote` split-brain non-overlap invariant is a first-class const-checked contract (3 compile-time asserts + tests over a TTL range). Evidence: 5 lease unit tests + `tests/ha_failover.rs` extended (`lease_timing_leaves_no_split_brain_window_and_is_faster`: the holder's demote deadline strictly precedes the challenger's promote deadline with a positive guard, and the lease is faster than + bounded by the legacy timings). Full lib + ha_failover green; clippy clean; guardrails satisfied. RECORDED FOLLOW-UP: wire the lease onto the live `ha_state` promotion loop (renew the epoch row's lease column, promote on lease expiry) — the risky mutation of the split-brain path, deferred by design. |

| WP-19 (slice 2) | **DONE** 2026-07-08 (durable lease mechanism + failover drill) | Make the WP-19 lease DURABLE and prove promotion-on-lease-expiry composes with the split-brain fence — the substantive middle of the live-loop wiring, deterministically verified. NO new column: the lease reuses the existing `ha_state` singleton — `epoch` + `active_instance_id` are the write-ownership identity, and the row's `updated_at_ms` becomes the holder's last-renew timestamp. New store ops in `src/verifier_store/epoch.rs`: `renew_lease(instance_id, held_epoch, now_ms)` — refreshes `updated_at_ms` ONLY if `epoch == held AND active_instance_id == instance_id` (the load-bearing epoch+holder guard: a superseded instance matches 0 rows → its renewal FAILS → that failure IS its self-demote signal, the durable analogue of lease expiry); `read_ha_lease → HaLease{epoch, holder, last_renew_ms}`. New durable-lease decision wrappers in `src/lease.rs` (`promotion_due_since_renew`/`holder_must_self_demote`) map the absolute `updated_at_ms` to the slice-1 elapsed helpers with a SATURATING subtraction, so cross-node clock skew fails SAFE (an observer reading behind the stored renewal sees elapsed 0 = fresh, never a spurious promote-over-a-live-holder). Evidence: `tests/ha_failover.rs` gains `lease_driven_failover_promotes_and_fences_using_the_durable_lease` — a deterministic two-store drill over the REAL ops: A claims + renews (B reads the fresh lease → stays standby); A dies → the durable lease goes stale past `promote_after` → B promotes via the real `try_claim_epoch` CAS + takes the lease; the revived old holder A's `renew_lease` is REFUSED by the epoch+holder guard (forcing self-demote) AND its actuator write is fenced — exactly one holder, no split brain. Plus a lease-wrapper unit test incl. the skew-fails-safe cases. 850 lib + 4 ha_failover green; clippy clean (touched files); guardrails untouched. RECORDED FOLLOW-UP (final step): wire these store ops into the live `standby_monitor` async loops — a renew sub-loop at the lease cadence + flipping the promotion-monitor trigger from the heartbeat timeout to `promotion_due_since_renew` (env-gated, with the epoch-CAS fence as the unchanged backstop) — which needs a live multi-node failover drill (real timers) to validate before it becomes default. |

| WP-20 (slice 2c) | **DONE** 2026-07-08 (deadline-miss observability) | The manifest's per-task `deadline_ms` budgets are now LIVE-OBSERVABLE on `/metrics` — the safe, no-new-dep piece of the s2c bundle (the other two, below, are deferred with cause). New `DeadlineRegistry` in `src/execution_manager.rs`: one lock-free `DeadlineStats` per manifest task that declares a budget (`from_manifest`; today only `telemetry_watchdog`, `deadline_ms=100`), `record(task, elapsed_ms)` (no-op for an unbudgeted/unknown task), and `append_prometheus` emitting `kirra_task_deadline_{cycles,misses}_total{task="…"}`. Wired live: `AppState` gained `deadline_registry: Arc<DeadlineRegistry>`; the `telemetry_watchdog` loop now TIMES each sweep on the CLOCK seam (real wall-ms under `SystemClock`; deterministic under a test `VirtualClock`) and records it (miss = a sweep past its 100 ms budget); `GET /metrics` appends the series (in-memory atomics, posture-exempt). Evidence: 16 execution_manager tests (3 new: budgeted-tasks-only coverage, record-only-budgeted, labelled-Prometheus-counters) + 857 lib green; verifier.rs 1253→1262 (ceiling bumped, panic budget UNCHANGED at 28 — the field init is `Arc::new(DeadlineRegistry::from_manifest(...))`, no unwrap/expect/panic); clippy + guardrails clean. RECORDED FOLLOW-UP: (i) feed a SUSTAINED miss into supervisor posture escalation (control-path, separate from this read-only observability); (ii) the SchedulingClass `SCHED_FIFO`/`sched_setaffinity` syscalls are BLOCKED by design on the tokio model — tokio tasks multiplex over shared worker threads, so per-task RT scheduling needs the loops on dedicated OS threads (the EPIC #270 QNX-partition lane), not a fake per-task syscall; (iii) driving the data-coupled worker/watchdog + HA loops through the registry (slice 2b's deferral) still needs the startup-path restructure + a live drill. |

| WP-20 (slice 2b) | **DONE** 2026-07-08 (manifest-driven monitor dispatch) | The resolved manifest order now DRIVES real spawns (not just validates at boot). New mechanism in `src/execution_manager.rs`: `SpawnRegistry<C>` (a name→spawn-action registry, generic over the spawn context so the ordering/drift logic is unit-tested with a mock) + `dispatch_in_order(registry, manifest, cover, ctx)` — invokes the registered actions in the manifest's RESOLVED dependency order, FAIL-CLOSED on any registry↔manifest drift (`DispatchError::{UnknownTask, MissingSpawner, DuplicateSpawner, Manifest}`): the registered spawners must EXACTLY cover their declared task set, so a spawn site can never silently diverge from its manifest entry. Wired live: the three trailing INDEPENDENT supervised monitors (`campaign_monitor`, `cert_expiry_monitor`, `audit_shipper`) in `wire_active_posture_freshness` are now dispatched via the registry instead of three hand-coded spawn blocks — the resolved cover order equals the prior hand-coded order (behaviour-preserving), and a drift (a monitor added to the manifest but not the registry, or vice versa) aborts startup. Evidence: 13 execution_manager tests (4 new: ordered dispatch via a recording mock ctx proving actions fire in RESOLVED not REGISTRATION order, the three drift refusals, unresolvable-manifest propagation) + 854 lib green; bin grew 5236→5263 (ceiling bumped, panic budget UNCHANGED at 150 — closures + dispatch + tracing + exit-on-drift, no unwrap/expect/panic); clippy clean; guardrails satisfied. RECORDED FOLLOW-UP (WP-20 slice 2c): drive the data-coupled posture worker + telemetry watchdog (thread `posture_tx` via the OnceCell) and the HA heartbeat/promotion loops through the registry too (needs the startup-path restructure + a live drill); apply `SchedulingClass` as real `SCHED_FIFO`/`sched_setaffinity` syscalls; feed `DeadlineStats` into `/metrics` + supervisor escalation. |

| WP-20 (slice 2a) | **DONE** 2026-07-08 (manifest boot gate) | The FIRST of WP-20's live-wiring follow-ups: `main()` now ADOPTS the execution manifest as a fail-closed BOOT GATE. Before any supervised loop is spawned, the Active/standby startup path resolves `execution_manager::TASK_MANIFEST` via `resolve_startup_order` and, on any `ManifestError` (duplicate / unknown-dep / cycle), logs + `std::process::exit(1)` — a malformed future manifest edit now ABORTS the service at boot (not merely a failed unit test), the same fail-closed discipline as SG-008; on success it logs the resolved order so the boot record documents the canonical supervised-loop sequence. Bin grew 5208→5236 (guardrail ceiling bumped with justification); panic budget UNCHANGED at 150 (resolve + tracing + exit, no unwrap/expect/panic). The resolver + manifest are the already-tested s1 core (9 execution_manager tests green); clippy clean; guardrails satisfied. RECORDED FOLLOW-UP (WP-20 slice 2b/2c): DRIVE the actual spawn sites from the resolved order via a name→spawn registry; apply `SchedulingClass` as real `SCHED_FIFO`/`sched_setaffinity` syscalls (degrade-with-warning where not permitted — needs a `libc`/`nix` dep + CAP_SYS_NICE); feed `DeadlineStats` into `/metrics` (`kirra_task_deadline_*`) + supervisor escalation (instrumenting the deadline-tracked loops). |

| WP-20 (slice 1) | **DONE** 2026-07-07 (execution-manager core) | Declarative execution manager (G-11), bounded low-risk core. New `src/execution_manager.rs`: `TASK_MANIFEST` — the 7 supervised background loops (posture-engine worker, telemetry watchdog, HA heartbeat/promotion, campaign/cert-expiry monitors, audit shipper) as DATA (name + deps + `Criticality` + `SchedulingClass` intent + optional per-cycle `deadline_ms`), replacing the implicit hand-coded order as documentation-as-code. `resolve_startup_order` — a topological sort of the dependency DAG (deps before dependents, deterministic declaration-order tiebreak), FAIL-CLOSED on a duplicate name / dependency on an unknown task / cycle (the same discipline as the fleet DAG's cycle→LockedOut). `deadline_missed` + `DeadlineStats` (lock-free per-task miss counter). Evidence: 8 tests — the REAL manifest self-validates (resolves, deps before dependents; a future typo'd/cyclic edit fails the build), linear chain, diamond DAG, cycle→refused, missing-dep→refused, duplicate→refused, deadline strict-over-budget, stats counting. Full lib suite green (machine-derived, do-not-hand-carry); clippy clean; guardrails satisfied. RECORDED FOLLOW-UP: adopt the resolved order in `main()`'s spawn sequence (abort on an unresolvable manifest), apply `SchedulingClass` as real syscalls (`sched_setaffinity`/`SCHED_FIFO` where permitted, degrade-with-warning), and feed `DeadlineStats` into `/metrics` + supervisor escalation — the parts that touch the running startup path, deferred by design. |

| WP-22 | **DONE** 2026-07-08 (EVT/MBPTA tail-fitting) | Probabilistic-WCET analysis, the G-3 SOFTWARE half (target measurement data stays external; the host-indicative-rule is UNCHANGED — pWCET is a host-side analysis of representative samples, never an FTTI claim). New feature-gated `crates/kirra-timing/src/evt.rs` (`evt = ["std"]`; OFF by default → the certifiable `no_std`/zero-alloc/integer-only core compiles out entirely). `fit_gpd_pot` — method-of-moments Generalized-Pareto fit over peaks-over-threshold exceedances, FAIL-CLOSED (`EvtError`) on too-few exceedances (`MIN_EXCEEDANCES = 30`), non-finite input, degenerate variance, or an unbounded/too-heavy tail (`xi >= 0.5`). `pwcet_return_level` — the GPD return-level quantile `x_p = u + (σ/ξ)·[(p/ζ)^(-ξ) − 1]` (ξ≈0 → the exponential limit), refusing a target probability outside the exceedance rate. `estimate_pwcet` — threshold-by-quantile → fit → return level + i.i.d./stationarity diagnostics (`lag1_autocorrelation`, `stationarity_split_mean_ratio`) so a non-representative sample is surfaced, not silently fit. `pwcet_converged` — a campaign-adequacy check (successive estimates within a rel-tol). Evidence: 18 kirra-timing tests green including `fit_recovers_known_gpd_params` (recovers a synthetic GPD's ξ/σ) and `pwcet_return_level_matches_the_analytic_exponential_quantile` (exact formula to 1e-6 + an end-to-end fit within an 8% honest fit-bias band at target p=1e-5); clippy clean under `--features evt`; `no_std` default build unaffected. Methodology doc §4a added (POT/GPD, return level, representativity gates, host-indicative-rule-unchanged, CI wcet-gate keeps its p99.9 structural guard). RECORDED FOLLOW-UP: ingest a real QNX-target-under-FIFO sample stream through `estimate_pwcet` and gate an FTTI budget on the pWCET return level (the target-data half of G-3, external to this repo). |

| WP-23 | **DONE** 2026-07-08 (statistical scenario campaign) | Seeded Monte-Carlo scenario campaign, the G-16 SOFTWARE half (the CARLA sim-host campaign runner stays external). Two new PURE modules in `crates/kirra-kpi-gate`: `confidence.rs` — proportion confidence intervals with no dependencies: `wilson_interval` (closed-form Wilson score) + `clopper_pearson_interval` (exact, via a self-contained regularized-incomplete-beta / Lanczos-`ln_gamma` inversion); both fail-closed on an empty sample (`[0,1]`). `montecarlo.rs` — a dependency-free `SplitMix64` PRNG driving `sample_doer_corpus`/`sample_perception_corpus`, which sample the SAME `EvalScenario`/`PerceptionCase` schema CONTINUOUSLY (ego speed × goal × hazard kind/distance/lateral; class × distance × span) so the corpus scales to 10⁴–10⁵ instead of the deterministic sweep's low hundreds. `run_montecarlo_gate` re-expresses each KPI floor as a CONFIDENCE-INTERVAL bound (`ci/scenario_kpi_montecarlo.json`): a must-be-small rate (`unsafe_miss_rate`) gates on its 95% Wilson UPPER bound, a must-be-large rate (admissibility, `hazard_recall`) on its LOWER bound, and the phantom-safety invariant on the POINT (never a CI slack). It CONSUMES the existing harnesses (`AdmissibilityTally` over the real checker, `SemanticEvalSummary`) and re-runs the #777 F1 negative-control fault families over the SAMPLED hazard frames, so the oracle-discrimination evidence survives resampling. Deterministic by seed. Two profiles: per-PR sampled subset (`ci.yml` gate step now sets `KIRRA_KPI_MC_PROFILE=per_pr`) + the full-corpus nightly run (new isolated `kpi-campaign-nightly.yml`, own `schedule` trigger so the whole CI graph is not fanned out). Evidence: 32 kirra-kpi-gate tests green (CI-math reference values incl. exact Clopper–Pearson degenerate/interior points, seed determinism, green+red campaign halves, negative-controls-under-sampling); the committed floors verified with margin (per-PR geometric admissibility Wilson.lo 0.964 vs 0.93 floor; `unsafe_miss` Wilson.hi 0.013 vs 0.03; recall/negctl Wilson.lo 0.985); clippy clean; end-to-end binary green in both profiles. RECORDED FOLLOW-UP: the CARLA campaign runner script (executes when a sim host exists — external to this repo). |

| WP-24 (slice 1) | **DONE** 2026-07-08 (ML lineage + OOD cores) | ML lineage + OOD monitor, the G-15 SOFTWARE half — the two PURE cores, in the lean `parko-core` (serde-only, no cross-workspace/crypto/hardware coupling). PART (b) OOD/input-shift monitor — new `parko/crates/parko-core/src/ood.rs`: a `CalibrationBaseline` (binned reference distribution of a model feature, e.g. per-frame detection confidence) + `OodMonitor::assess`, which bins a live window and computes the **Population Stability Index** (`PSI = Σ (live−base)·ln(live/base)`) vs the baseline, mapping the standard PSI bands (<0.10 stable / 0.10–0.25 moderate / ≥0.25 severe) to an escalation-only `SafetyPosture` recommendation the runtime folds in via `posture.escalate(recommended)`. DERATE-ONLY + FAIL-CLOSED per the doer-checker invariant: a non-finite feature (a broken model output) → `LockedOut`; an under-filled window (no evidence this tick) → `Nominal` no-op (never flaps); construction fail-closed on too-few calibration samples / bad binning / non-finite. PART (a) model lineage — new `parko/crates/parko-core/src/model_lineage.rs`: a `ModelLineage` state machine over the existing `#G16` `verify_model_file` verdict — a clean verified model COMMITS and becomes the last-good anchor; a rejected model `Rollback`s to the last-good digest; a rejection with NO known-good `Deny`s (deny-by-default); an unhashable/enforcement-off case handled explicitly (unverified never anchors). Evidence: 16 new parko-core tests (270 lib total green) — OOD in-distribution stays Nominal, moderate shift → Degraded, severe shift → LockedOut, non-finite → LockedOut, under-filled → no-op, plus a **false-positive-budget** test (≤2% escalations over 500 same-distribution windows) and PSI(identical)=0; lineage commit/rollback/deny/anchor-advance/seeded-good/unhashable-deny. Clippy clean (new files). RECORDED FOLLOW-UP: bind the allow-list to a SIGNED (Uptane targets, WP-13/`kirra-release-token`) manifest rather than a raw digest set (cross-workspace crypto plumbing); wire the lineage decision into the backend `load_model` (actually re-load the last-good artifact on `Rollback`) and feed the OOD monitor real per-tick confidences in `parko-ros2`'s `run_pipeline_tick` (escalate the effective posture); the Jetson CI runner (external). |

| WP-24 (slice 2a) | **DONE** 2026-07-08 (signed model manifest) | Bind the parko model-integrity allow-list to a **signed** manifest, reusing the WP-13 Uptane targets machinery — the headline of s2, delivered in the root workspace where the crypto (`kirra-release-token::uptane`) already lives, so there is NO cross-workspace crypto dep. A "model manifest" IS an Uptane `targets` metadata whose entries are the authorized model artifacts (SHA-256 digest + version); a node verifies it through the existing full chain (`verify_update` — timestamp→snapshot→targets: role separation, freshness, rollback floor, no mix-and-match), then DERIVES the authorized digest set. New pure `crates/kirra-release-token/src/model_targets.rs`: `authorized_model_digests` / `model_allowlist_env_value` (the comma-separated `KIRRA_MODEL_ALLOWLIST` value parko's `ModelAllowList::parse` already consumes) / `authorized_model_entry` / `is_model_authorized`. The projection takes an already-`VerifiedUpdate` (only obtainable from the fail-closed `verify_update`), so the type system guarantees ONLY a cryptographically-verified manifest can drive an allow-list; deny-by-default composes end to end (no verified manifest → empty allow-list → parko strict mode denies every model). This makes the allow-list a SIGNED FACT, not the operator ASSERTION the #G16 raw allow-list left it. Evidence: 4 new tests (allow-list = ordered signed digest set; digest-scoped authorization lookup; empty-manifest → empty allow-list; **the end-to-end signed-manifest → allow-list happy path + a manifest tampered AFTER signing is refused by the real chain**) + 45 release-token lib green; clippy clean. RECORDED FOLLOW-UP (parko-side, hardware/ros2-gated): the thin node step that sets `KIRRA_MODEL_ALLOWLIST` from the verified manifest for the co-located parko process; wire the backend `load_model` to the `ModelLineage` rollback; feed the OOD monitor live per-tick confidences in `run_pipeline_tick`; the Jetson CI runner (external). |

Wave 3 remaining: SUPERSEDED by the §0 verified-status re-baseline (2026-07-08) — see the ROI-ranked roadmap there. Summary: WP-21 in-line path first; WP-19 live lease flip (behind a drill); WP-24 orphan-core wiring; true MC/DC; WP-11 + RSS constant validation; one real PG driver + CI container (then stop trait extraction). Retired: SchedulingClass syscalls under tokio; whole-store trait completion; G-22; CRL-file.

## 3. Wave 0 — Hardening sprint (6 WPs, ~1–2 weeks elapsed)

Independent, high-ROI, low-risk. Can run as parallel sessions.

### WP-01 · Seqlock weak-memory fix + loom model — closes G-13
- **Change:** insert the acquire fence after the body copy and before the `g2` re-read in `crates/kirra-contract-channel/src/seqlock.rs` (`core::sync::atomic::fence(Acquire)`); document the aarch64 rationale at the site.
- **Evidence:** new loom model in `crates/kirra-loom-models` exercising writer/reader interleavings of the odd/even protocol (torn-read never accepted; retry-exhaustion fail-closed reachable); keep the 20k stress test.
- **Acceptance:** loom lane green with the new model; byte-layout compile-time asserts untouched.
- **Effort:** S. **Risk:** none (strictly strengthens ordering).

### WP-02 · Wire the fleet ingress rate limiter — closes G-10 (fleet half)
- **Change:** call `IngressRateLimiter::allow()` at the top of `recv_report` (`transport.rs:201`) and `recv_and_ingest` (`transport.rs:236`), **before** signature verification; surface `RejectReason::RateLimited` through the existing `RejectionCounter`.
- **Evidence:** flood test proving N+1-th message in a window is dropped pre-verify (assert verify not invoked — e.g. count verify calls via test signer); global-backstop test under spoofed source ids.
- **Acceptance:** zero behavior change under nominal rates (existing tests untouched).
- **Effort:** S.

### WP-03 · HTTP control-plane backpressure — closes G-10 (HTTP half)
- **Change:** tower layers on the assembled router: global + per-route concurrency limits, request-body size caps, load-shed → 429/503 with `Retry-After`. **Design constraint:** `/health`, `/ready`, `/metrics` stay exempt (scrape must survive load, mirroring the posture-exemption precedent); the actuator route sheds *closed* (a shed command is a denied command, which the consumer already treats as safe-stop per #405).
- **Evidence:** integration test flooding a mutation route → 429s while `/health` stays 200; SSE lag behavior documented.
- **Effort:** M.

### WP-04 · CI verification ratchets — closes G-6 (non-Ferrocene slices)
- **Change:** (a) `cargo fmt --check` lane; (b) fuzz lane upgraded from build-only to run-each-target N seconds (bounded, seed-corpus committed); (c) cargo-deny advisories gating with a documented triage allowlist; (d) coverage lane gains a ratchet threshold (start at current %, fail on regression — same philosophy as `check_quality_guardrails.py`).
- **Evidence:** the lanes themselves; a deliberately-broken canary branch is NOT merged, just verified locally.
- **Effort:** S–M.

### WP-05 · Observability: histograms + spans — closes G-10 (observability half)
- **Change:** latency histograms (fixed buckets, fixed label cardinality per existing metrics discipline) for verdict path, store ops, HTTP request duration; `#[instrument]` spans with a per-request correlation id propagated into audit entries where one exists.
- **Constraint:** zero allocation added to `validate_vehicle_command` itself — instrument at the middleware boundary, not inside the pure kernel (WCET gate must stay byte-identical on the kernel).
- **Evidence:** `/metrics` shows `_bucket` series; wcet-gate lane unchanged.
- **Effort:** M.

### WP-06 · Safety-doc consistency pass — reduces G-5 friction (docs only)
- **Change:** unify the 16-goal kernel + 9-goal Occy schemes into one indexed catalog (keep both IDs, one authoritative table); purge the stale "306 passing" figure from the ~5 downstream docs; finish the AEGIS→KIRRA ID rebrand in doc headers.
- **Acceptance:** `grep -r "306 passing"` empty; SAFETY_CASE_INDEX cross-links resolve.
- **Effort:** S.

---

## 4. Wave 1 — Safety-kernel correctness (5 WPs, ~2–6 weeks elapsed)

The checker is the product; these WPs make its strongest claims true.

### WP-07 · RSS lateral primitive completion (#408) — closes G-12 (params half)
- **Change:** split `lat_accel_max` into `a_lat_accel_max` / `a_lat_brake_min` per IEEE 2846 §5.2 (resolving the non-monotonic single-parameter collapse documented at `rss.rs:111-141`); add the μ lateral-fluctuation margin as an explicit additive term; parameterize the hardcoded 2.5 m footprint band from the vehicle-class contract (`KIRRA_VEHICLE_CLASS` → footprint), keeping the current constant as the robotaxi value.
- **Constants discipline:** all new numbers tagged `VALIDATION-PENDING` exactly like `contract_profiles.rs:23` — the *code shape* closes here; sign-off is the external track.
- **Evidence:** proptest properties — new bound ≥ old bound everywhere (strict conservatism), monotonicity in each param, fail-safe on non-finite; MC/DC pair-completion tests extended to the new decisions; update `KIRRA_RSS_FORMAL_SPECIFICATION.md` §2.
- **Effort:** M. **Dependency:** none.

### WP-08 · Mutation-testing gate — closes G-6 (mutation slice)
- **Change:** `cargo-mutants` CI lane scoped to the checker crates (`kirra-core` kinematics, `kirra-trajectory`, `parko-core` rss/detector) with a survivors baseline file (ratchet: no NEW survivors); triage and kill the initial survivor set with targeted tests.
- **Evidence:** the lane + a `docs/testing/MUTATION_BASELINE.md` documenting accepted survivors with justification (mirrors the panic-budget pattern).
- **Effort:** M (survivor triage dominates).

### WP-09 · Divergence → durable fleet posture — closes G-14
- **Change:** implement `STAGE_S-DG1_DIVERGENCE_POSTURE.md`: the comparator's `recommended_posture` escalation, when sustained past the existing accumulator lockout level, reports into the verifier (existing identity-gated report route or a typed `PostureRecalcTrigger`) and latches the durable sticky `LockedOut` (human-reset). Route through `PostureEngineSender` (never direct recalc — CLAUDE.md).
- **Evidence:** integration test: sustained injected divergence → fleet `LockedOut` survives verifier restart (generation persistence); transient divergence → no latch (hysteresis preserved); loom model if a new cross-thread latch protocol is introduced.
- **Doc:** flip COMPARATOR_DIVERSITY.md §status from "wiring PROPOSED" to implemented-pending-signoff.
- **Effort:** M. **Dependency:** none (parko + verifier seams both exist).

### WP-10 · Feed pedestrian RSS from Taj — closes the armed-but-unfed half of G-4
- **Change:** heuristic VRU classifier in `kirra-taj` over existing clusters (extent + speed envelope + persistence → `PerceivedPedestrian` with honest `age_s`); plumb the channel through `AdaptorState` → `node.rs` (replacing the hardcoded `None` at `node.rs:708-713`), env-gated like the other slow-loop features (default off → byte-identical, per the doer-checker invariant).
- **Fail-closed rule:** classifier *uncertainty* promotes TO pedestrian (the stricter bound), never away from it; a silent/stale VRU channel with the gate enabled → MRC-floor cap, mirroring the redundancy monitor.
- **Evidence:** scenario tests through the real checker (crossing pedestrian → rejection/derate; clear corridor → admitted); kpi-gate rows with a negative-control mutation.
- **Effort:** M–L. **Dependency:** none.

### WP-11 · Curved-geometry RSS (Frenet along the lane corridor) — closes G-12 (geometry half)
- **Change:** longitudinal/lateral decomposition along the `LaneCorridor` centerline arc-length instead of straight-lane axes; straight-lane remains the degenerate case (byte-identical on straight corridors — regression-tested).
- **Evidence:** curved-lane scenarios where straight-lane RSS under/over-triggers, proving the fix; conservatism property vs the straight-lane bound on synthetic arcs.
- **Effort:** L. **Dependency:** WP-07 (build on the completed lateral primitive). Schedule late in wave or slip to Wave 3.

---

## 5. Wave 2 — OTA & security depth (5 WPs, ~4–8 weeks elapsed)

Pure software; converts "R156-shaped" into defensible depth.

### WP-12 · Signature-verified staging — closes G-7 (first slice)
- **Change:** extend `verify_staged_artifact` (`kirra-ota-installer/src/lib.rs:321`) to require an Ed25519 signature over the artifact digest, verified against the pinned governor release key (`kirra-release-token` seam); campaign records carry the signature; assignment GET serves it; hash-only becomes a rejected legacy mode.
- **Evidence:** tamper tests — right hash/wrong sig, wrong key, sig-over-different-digest all refuse to arm the slot; two_node_rollout drill extended.
- **Effort:** M. **Dependency:** none.

### WP-13 · Uptane role metadata — closes G-7 (core)
- **Change:** implement the four-role model in `kirra-release-token` + the campaign engine: `root` (key rotation authority), `targets` (artifact digests), `snapshot` (consistency), `timestamp` (freshness); metadata expiry enforced fail-closed on the node's `decide_pull` path; rollback/freeze-attack protection via monotonic versions (reuse the generation-persistence pattern).
- **Evidence:** attack-scenario test suite — replayed old targets, frozen timestamp, forked snapshot, rotated-root recovery after simulated targets-key compromise; store tables for role metadata with the existing audit-chain treatment.
- **Effort:** L (largest WP in the plan; split root/targets from snapshot/timestamp into two PRs).
- **Dependency:** WP-12.

### WP-14 · Governor key rotation + revocation — closes G-7 (lifecycle)
- **Change:** rotation flow for the release identity (new key signed by root role from WP-13; nodes accept during an overlap window; old key revocable), audit-chained like the existing audit-signing-key rotation route.
- **Evidence:** rotation drill test — fleet mid-campaign, key rotates, staged nodes still verify, revoked-key artifacts refuse.
- **Effort:** M. **Dependency:** WP-13.

### WP-15 · mTLS cert lifecycle — closes G-19
- **Change:** expiry tracking + renewal seam for `cert_principals` (expiry column, `/system/cert-principals` renewal op, startup + periodic expiry warnings via metrics/audit), CRL-file or explicit-revocation check at the TLS verifier callback.
- **Evidence:** expired-cert rejected, renewed-cert accepted without restart, revocation honored on next handshake.
- **Effort:** M.

### WP-16 · Measured-boot software completion — closes G-8 (software half)
- **Change:** provisioning CLI (`kirra-ota-ctl` subcommand or sibling) that enrolls a node's AK + PCR16 + `require_tpm_quote=true` in one audited operation against a real-or-simulated TPM (use `swtpm`-compatible flows so it's sandbox-testable); make quote-required the documented default for new registrations behind an env gate.
- **Evidence:** end-to-end challenge→quote→verify test against the existing `tpm_quote` parser with generated fixtures; docs update to stop overstating deployment reality (per MGA §4 G-8).
- **Effort:** M. **External remainder:** Orin Secure Boot + dm-verity + physical TPM — hardware track.

---

## 6. Wave 3 — Platform & scale (8 WPs, ~2–5 months elapsed)

### WP-17 · Config unification — closes G-17
- Typed `KirraConfig` for verifier + ros2 node using the gateway config pattern (versioned, validated, effective-config SHA-256 digest logged to the audit chain at startup); every `KIRRA_*` env var becomes a documented field with a fail-closed parse; unknown-var warning sweep.
- **Evidence:** invalid-config abort tests per field class; digest appears in audit; ENVIRONMENT.md regenerated from the schema (single source of truth).
- **Effort:** M–L.

### WP-18 · Store abstraction + migrations + Postgres backend — closes G-9 (store half) + G-20
- Extract a `VerifierStorage` trait from `VerifierStore` (per-table submodules make this tractable); SQLite impl unchanged and default; Postgres impl behind a feature; versioned migration framework (`user_version` for SQLite, migrations table for PG) with CI upgrade tests from the last released schema.
- **Constraint:** INVARIANT #12 (disk-before-memory ordering) holds for every backend; the epoch fence semantics must be reproduced transactionally in PG (`SELECT … FOR UPDATE`).
- **Evidence:** the full store test suite (88 tests) runs against both backends in CI (PG via service container); ha_failover + audit power-loss drills stay SQLite (file-topology-specific) and are documented as such.
- **Effort:** L. Split: trait extraction PR → migration framework PR → PG backend PR.

### WP-19 · Lease-based failover — closes G-21, completes G-9
- Replace heartbeat-poll promotion with a store-backed lease (TTL ≤ posture cache TTL, renewed at half-life, CAS on the existing epoch machinery); demotion window provably closes before promotion window opens (extend the existing pure invariant in `tests/ha_failover.rs`).
- **Evidence:** failover drill target ≤5 s detection; split-brain fence tests preserved.
- **Effort:** M. **Dependency:** WP-18 trait (so the lease is backend-portable).

### WP-20 · Execution manager — closes G-11
- Declarative task manifest (TOML in the unified config): per-background-task priority/affinity (Linux `sched_setaffinity`/`SCHED_FIFO` where permitted, degrade-with-warning where not), startup dependency DAG (replacing implicit ordering in `main`), per-task deadline-miss counters feeding metrics + supervisor escalation.
- **Evidence:** startup-order test (dependency violated → abort); deadline-miss counter fires under injected stall; existing supervisor restart-budget tests preserved.
- **Effort:** L.

### WP-21 · In-line SHM enforcement path (host-verified) — closes G-1 software half; WP-21b closes G-18 slice
- Assemble the already-built parts into the deployable shape ADR-0006/0030 promise: a governor process consuming proposals via `kirra-hv-carrier` SHM seqlock, verdicting with the pure kernel, emitting release tokens out-of-band (per the 91 µs finding), with the HTTP plane demoted to management. Extend `kirra-l3-e2e` from drill to long-running service mode with graceful degradation.
- **21b:** feature-gated iceoryx2 transport implementing the same `ContractReader/Writer` seam (spike → SDK behind `iceoryx2` feature, keeping the isolation rule for default builds).
- **Evidence:** host soak test (hours, zero missed MRC publications), fault-injection rows from HV_FAULT_CAMPAIGN that don't need the hypervisor (writer kill, stale generation, torn-write simulation); timing stays labeled INDICATIVE.
- **Effort:** L–XL. **Dependency:** WP-01. **External remainder:** QNX/hypervisor/target = hardware track, which inherits this code unchanged.

### WP-22 · EVT/MBPTA in kirra-timing — closes G-3 software half
- Add extreme-value-theory tail fitting (GEV/GPD over block maxima / peaks-over-threshold), i.i.d./stationarity checks, and convergence criteria to `kirra-timing`; `kirra-wcet-bench` reports pWCET curves alongside HWM, all still env-tagged INDICATIVE on host.
- **Evidence:** statistical unit tests against synthetic distributions with known tails; methodology doc §4 updated; the CI wcet-gate keeps p99.9 (no gate change until target data exists).
- **Effort:** M.

### WP-23 · Statistical scenario campaign — closes G-16 software half
- Procedural scenario generator scaling the KPI corpus to 10⁴–10⁵ (parameterized sweeps + seeded Monte-Carlo sampling over the existing scenario schema); Wilson/Clopper-Pearson confidence intervals on `unsafe_miss_rate`/`hazard_recall` in `kirra-kpi-gate`; CI runs a sampled subset per-PR + full corpus nightly; CARLA campaign runner script (executes when a sim host exists — external).
- **Evidence:** gate thresholds re-expressed as CI upper bounds, negative-control rows preserved; corpus generation deterministic by seed.
- **Effort:** L.

### WP-24 · ML lineage + OOD — closes G-15 software half
- (a) Model integrity flips from opt-in-logging to deny-by-default under a deployment profile, with signed model manifests (reuse WP-13 targets-role machinery) and rollback to last-good; (b) OOD/input-shift monitor (activation/confidence-distribution drift vs a calibration baseline) feeding the existing posture-escalation accumulator — derate-only, fail-closed on monitor absence per the doer-checker invariant.
- **Evidence:** substituted-model refused; injected distribution shift escalates posture in the tick-pipeline test rig; nominal corpus does not (false-positive budget test).
- **Effort:** L. **External remainder:** Jetson CI runner.

---

## 7. Sequencing & dependency summary

```
Wave 0 (parallel): WP-01  WP-02  WP-03  WP-04  WP-05  WP-06
Wave 1:            WP-07 ──► WP-11        WP-08   WP-09   WP-10
Wave 2:            WP-12 ──► WP-13 ──► WP-14     WP-15   WP-16
Wave 3:            WP-17    WP-18 ──► WP-19      WP-20   WP-22   WP-23   WP-24
                   WP-01 ──► WP-21 (◄ WP-13 for release-token roles, soft)
```

Suggested cadence: Wave 0 as an immediate sprint; then interleave one Wave-1 safety WP with one Wave-2 security WP per review cycle (they touch disjoint crates, so sessions parallelize cleanly); Wave 3 starts once WP-13 and WP-07 merge. Total plan volume ≈ **60–90 Claude Code sessions** across ~24 PR-sized units.

## 8. What this plan deliberately does NOT claim

Completing every WP makes the *software* production-shaped and leaves the following honestly open (external tracks, per the user's carve-out): QNX Hypervisor procurement + interference campaign (G-2), target-silicon WCET rows (G-3), physical secure boot/TPM enrollment (G-8), Jetson CI runner (G-15), real sensor/perception integration or D1 hardware (G-4), Ferrocene (G-6), and all certification-body engagement (G-5). Each corresponding WP is designed so the external track starts with its software dependency already merged, tested, and labeled.
